// Our fork, not upstream. These links are shown to users, and pointing them at the
// original repository both leaked whose product this started as and sent people to a
// tree that does not contain the code they are running.
export const GITHUB_REPO_OWNER = "orchids285-sketch";
export const GITHUB_REPO_NAME = "reacher-x";
export const GITHUB_REPO_URL = `https://github.com/${GITHUB_REPO_OWNER}/${GITHUB_REPO_NAME}`;
export const GITHUB_REPO_ISSUES_URL = `${GITHUB_REPO_URL}/issues`;
